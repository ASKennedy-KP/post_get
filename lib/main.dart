import 'package:flutter/material.dart';
import 'package:sesi9/sectionCHOICE.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: SectionChoice(),
    );
  }
}
